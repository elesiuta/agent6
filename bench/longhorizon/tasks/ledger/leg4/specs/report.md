# report

    report YYYY-MM

For every account with entries in that month, one line `ACCOUNT TOTAL MEAN`,
accounts sorted by name: TOTAL is the sum of the account's entries in the
month, MEAN the mean entry amount, both rendered to the cent the way the rest
of the ledger rounds. Entries of other months are ignored.
